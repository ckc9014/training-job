apiVersion: v1
kind: Service
metadata:
  name: training-job-service
  namespace: training
  labels:
    app: training-job
spec:
  selector:
    app: training-job 
  ports:
    - name: metrics
      protocol: TCP
      port: 8080
      targetPort: 8080